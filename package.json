{
  "name": "bettertext-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev:demo": "npm run dev --workspace=@bettertext/babylon-tmp-text-demo",
    "build:runtime": "npm run build --workspace=@bettertext/babylon-tmp-text"
  },
  "devDependencies": {}
}
